import React from "react";
import main from '../Images/main.jpg';

const Hero = () => {
    return (
        <section className="left">
            <img src={main} alt="hero" />
        </section>
    );
};

export default Hero;
